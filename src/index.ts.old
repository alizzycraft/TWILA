declare var tapestry: any;

// Absolute minimal test - only mod.define call
tapestry.mod.define({
  id: "twila",
  version: "1.0.0",
  onLoad: function() {
    console.log("Twila mod loaded successfully!");
  }
});
